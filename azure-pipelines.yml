trigger:
  branches:
    include:
      - main

pool:
  vmImage: "ubuntu-latest"

resources:
  repositories:
    - repository: Playwright_Tests_Repo
      type: github
      name: sd576/Playwright-API-Tests
      ref: refs/heads/main
      endpoint: GitHub_Connection

steps:
  # ✅ Checkout the Repository
  - checkout: self

  # ✅ Set Node.js Version
  - task: UseNode@1
    inputs:
      version: "20"
    displayName: "Set Node.js to v20"

  # ✅ Install Dependencies
  - script: |
      npm ci
      npx playwright install
    displayName: "Install Dependencies and Playwright"

  # ✅ Lint and Format Code
  - script: |
      npm run lint
      npm run format
    displayName: "Run ESLint and Prettier Checks"

  # ✅ Seed the Database and Start the API Server in the Background
  - script: |
      npm run setup-and-start &
      echo "Server starting in the background..."
    displayName: "Seed Database and Start Server"

  # ⏳ Wait for the Server to be Ready
  - script: |
      for i in {1..10}; do
        if curl --silent http://localhost:3000/api-docs; then
          echo "Server is ready!"
          exit 0
        else
          echo "Waiting for server... Attempt $i"
          sleep 5
        fi
      done
      echo "Server failed to start in time."
      exit 1
    displayName: "Wait for Server to Start"

  # ✅ Debugging Step: Print Working Directory and List All Files
  - script: |
      pwd
      ls -R
    displayName: "Print Working Directory and List All Files"

  # ✅ List Test Files in Correct Path (Adjust After Checking Directory Structure)
  - script: ls -R src/tests/api
    displayName: "List Test Files in src/tests/api"

  # 🏃 Run Playwright Tests with a Different Port (4000)
  - script: |
      export CI=true
      PORT=4000 NODE_ENV=playwright npx playwright test src/tests/api --reporter=junit --output=playwright-results
    displayName: "Run Playwright Tests"

  # ✅ Publish Playwright JUnit Test Results
  - task: PublishTestResults@2
    inputs:
      testResultsFiles: "playwright-results/*.xml"
      testRunTitle: "Playwright Tests"
    displayName: "Publish Playwright Test Results"
