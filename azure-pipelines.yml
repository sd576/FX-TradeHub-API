trigger:
  branches:
    include:
      - main

pool:
  vmImage: "ubuntu-latest"

variables:
  SYSTEM_DEBUG: true # Enable detailed debug logging

steps:
  # ✅ Checkout the Repository
  - checkout: self

  # ✅ Set Node.js Version
  - task: UseNode@1
    inputs:
      version: "20"
    displayName: "Set Node.js to v20"

  # ✅ Install Dependencies
  - script: |
      npm ci
      echo "✅ Dependencies installed successfully."
    displayName: "Install Dependencies"

  # ✅ Debugging Step: List Installed Node Modules
  - script: ls -R node_modules
    displayName: "List Installed Node Modules"

  # ✅ Check if @playwright/test is Installed
  - script: |
      if [ -d "node_modules/@playwright/test" ]; then
        echo "✅ @playwright/test is installed."
      else
        echo "❌ @playwright/test is missing!"
        exit 1
      fi
    displayName: "Check if @playwright/test is Installed"

  # ✅ Install Playwright System Dependencies and Browser Binaries
  - script: |
      npx playwright install-deps
      npx playwright install
      echo "✅ Playwright and required system dependencies installed."
    displayName: "Install Playwright and System Dependencies"

  # ✅ Seed the Database and Start the API Server in the Background
  - script: |
      npm run setup-and-start &
      echo "Server starting in the background..."
    displayName: "Seed Database and Start Server"

  # ⏳ Wait for the Server to be Ready
  - script: |
      for i in {1..10}; do
        if curl --silent http://localhost:3000/api-docs; then
          echo "Server is ready!"
          exit 0
        else
          echo "Waiting for server... Attempt $i"
          sleep 5
        fi
      done
      echo "Server failed to start in time."
      exit 1
    displayName: "Wait for Server to Start"

  # 🏃 Run Playwright Tests
  - script: |
      export CI=true
      PORT=4000 NODE_ENV=playwright npx playwright test src/tests/api --reporter=junit --output=playwright-results
    displayName: "Run Playwright Tests"

  # ✅ Publish Playwright JUnit Test Results
  - task: PublishTestResults@2
    inputs:
      testResultsFiles: "playwright-results/*.xml"
      testRunTitle: "Playwright Tests"
    displayName: "Publish Playwright Test Results"
